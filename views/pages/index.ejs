<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>

    <main>
      <div class="container">

        <h1>Demo Payment Gateway</h1>

        <form class="form-horizontal" method="post" action="/">

          <h2 class="form-heading">Make a payment</h2>

          <% if (failureMessage.length > 0) { %>
            <div class="alert alert-danger"><%= failureMessage %></div>
          <% } %>

          <% if (successMessage.length > 0) { %>
            <div class="alert alert-success"><%= successMessage %></div>
          <% } %>

          <h3>Order</h3>

          <div class="form-group">
            <label for="price" class="col-sm-2 control-label">Price</label>
            <div class="col-sm-10">
              <input type="number" pattern="[0-9]*[.,]?[0-9]+" inputmode="numeric" step="0.01" min="0.01" name="price" id="price" class="form-control" placeholder="How much would you like to pay?" required autofocus>
            </div>
          </div>

          <div class="form-group">
            <label for="currency" class="col-sm-2 control-label">Currency</label>
            <div class="col-sm-10">
              <select id="currency" name="currency" class="form-control" required>
                <option value="" selected disabled>Please choose...</option>
                <option value="aud">AUD</option>
                <option value="eur">EUR</option>
                <option value="hkd">HKD</option>
                <option value="sgd">SGD</option>
                <option value="thb">THB</option>
                <option value="usd">USD</option>
              </select>
            </div>
          </div>

          <div class="form-group">
            <label for="full-name" class="col-sm-2 control-label">Full Name</label>
            <div class="col-sm-10">
              <input type="text" id="full-name" name="full-name" class="form-control" placeholder="Type your full name" required>
            </div>
          </div>

          <h3>Payment</h3>

          <div class="form-group">
            <label for="card-holder-name" class="col-sm-2 control-label">Card Holder's Name</label>
            <div class="col-sm-10">
              <input type="text" id="card-holder-name" name="card-holder-name" class="form-control" placeholder="Your name as it appears on your card" required>
            </div>
          </div>

          <div class="form-group">
            <label for="card-number" class="col-sm-2 control-label">Credit Card Number</label>
            <div class="col-sm-10">
              <input type="text" pattern="[0-9]{13,16}" id="card-number" name="card-number" class="form-control" placeholder="Numbers only - 123456789012345" required>
            </div>
          </div>

          <div class="form-group">
            <label for="card-expiration" class="col-sm-2 control-label">Credit Card Expiration</label>
            <div class="col-sm-10">
              <input type="text" pattern="(0[1-9]|1[012])\/20[1-9]{2}" id="card-expiration" name="card-expiration" class="form-control" placeholder="mm/yyyy" required>
            </div>
          </div>

          <div class="form-group">
            <label for="card-ccv" class="col-sm-2 control-label">Credit Card CCV</label>
            <div class="col-sm-10">
              <input type="text" pattern="[0-9]{3,4}" id="card-ccv" name="card-ccv" class="form-control" placeholder="A 3 or 4 digit number on your card" required>
            </div>
          </div>

          <button class="btn btn-lg btn-primary btn-block" type="submit">Submit</button>
        </form>

      </div>
    </main>

    <footer>
      <% include ../partials/footer %>
    </footer>

  </body>
</html>
