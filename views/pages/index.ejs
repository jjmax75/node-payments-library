<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>

    <main>
      <div class="container">

        <h1>Demo Payment Gateway</h1>
        
        <form class="form-horizontal" method="post" action="/">

          <h2 class="form-heading">Make a payment</h2>

          <% if (failureMessage.length > 0) { %>
            <div class="alert alert-danger"><%= failureMessage %></div>
          <% } %>

          <% if (successMessage.length > 0) { %>
            <div class="alert alert-success"><%= successMessage %></div>
          <% } %>

          <h3>Order</h3>

          <div class="form-group">
            <label for="price" class="col-sm-2 control-label">Price</label>
            <div class="col-sm-10">
              <input type="number" pattern="[0-9]*[.,]?[0-9]+" inputmode="numeric" step="0.01" min="0.01" name="price" id="price" class="form-control" placeholder="How much would you like to pay?" required autofocus>
            </div>
          </div>

          <div class="form-group">
            <label for="currency" class="col-sm-2 control-label">Currency</label>
            <div class="col-sm-10">
              <select id="currency" name="currency" class="form-control" required>
                <option value="" selected disabled>Please choose...</option>
                <option value="AUD">AUD</option>
                <option value="EUR">EUR</option>
                <option value="HKD">HKD</option>
                <option value="SGD">SGD</option>
                <option value="THB">THB</option>
                <option value="USD">USD</option>
              </select>
            </div>
          </div>

          <div class="form-group">
            <label for="fullName" class="col-sm-2 control-label">Full Name</label>
            <div class="col-sm-10">
              <input type="text" id="fullName" name="fullName" class="form-control" placeholder="Type your full name" required>
            </div>
          </div>

          <h3>Payment</h3>

          <div class="form-group">
            <label for="cardHolderName" class="col-sm-2 control-label">Card Holder's Name</label>
            <div class="col-sm-10">
              <input type="text" id="cardHolderName" name="cardHolderName" class="form-control" placeholder="Your name as it appears on your card" required>
            </div>
          </div>

          <div class="form-group">
            <label for="cardNumber" class="col-sm-2 control-label">Credit Card Number</label>
            <div class="col-sm-10">
              <input type="text" pattern="[0-9]{13,16}" id="cardNumber" name="cardNumber" class="form-control" placeholder="Numbers only - 123456789012345" required>
            </div>
          </div>

          <div class="form-group">
            <label for="cardExpiration" class="col-sm-2 control-label">Credit Card Expiration</label>
            <div class="col-sm-10">
              <input type="text" pattern="(0[1-9]|1[012])\/20[1-9]{2}" id="cardExpiration" name="cardExpiration" class="form-control" placeholder="mm/yyyy" required>
            </div>
          </div>

          <div class="form-group">
            <label for="cardCcv" class="col-sm-2 control-label">Credit Card CCV</label>
            <div class="col-sm-10">
              <input type="text" pattern="[0-9]{3,4}" id="cardCcv" name="cardCcv" class="form-control" placeholder="A 3 or 4 digit number on your card" required>
            </div>
          </div>

          <button class="btn btn-lg btn-primary btn-block" type="submit">Submit</button>
        </form>

      </div>
    </main>

    <footer>
      <% include ../partials/footer %>
    </footer>

  </body>
</html>
